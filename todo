when items checked update the is bkd or not status
make remove button on files remove the file from the folder
condence the filelist into groups by game
notify on all items shouldn't notify on page load as it doesn't make sense to do so
